namespace GiftLists.Application.GiftLists.RemoveGiftItem;

/// <summary>No fields to carry — see <c>DeleteGiftListResponse</c> for why this is an empty record rather than non-generic <c>Result</c>.</summary>
public sealed record RemoveGiftItemResponse;
